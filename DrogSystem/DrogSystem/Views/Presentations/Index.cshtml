@model IEnumerable<DrogSystem.Models.Presentation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PresentationType.NombrePresentacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CantPresentacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PresentationType.NombrePresentacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CantPresentacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PresentationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PresentationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PresentationId })
        </td>
    </tr>
}

</table>
